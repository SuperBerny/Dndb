router.get('/path to view', (req, res, next) => {
   res.render('path to file', {
      layout: 'looks in layouts dir',
      path: '/path to page',
      pageTitle: 'added to <title> {{ pageTitle }} <title>'
   });
});

